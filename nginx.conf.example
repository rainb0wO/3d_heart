# Nginx配置文件示例
# 复制此文件到 /etc/nginx/sites-available/particle-heart
# 然后创建软链接: sudo ln -s /etc/nginx/sites-available/particle-heart /etc/nginx/sites-enabled/

# HTTP重定向到HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com www.your-domain.com;  # 替换为你的域名

    # 重定向到HTTPS
    return 301 https://$server_name$request_uri;
}

# HTTPS配置
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name your-domain.com www.your-domain.com;  # 替换为你的域名

    # SSL证书路径（使用Let's Encrypt）
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # SSL优化配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # 网站根目录
    root /var/www/particle-heart;
    index index.html;

    # 日志
    access_log /var/log/nginx/particle-heart-access.log;
    error_log /var/log/nginx/particle-heart-error.log;

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1000;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/rss+xml
        font/truetype
        font/opentype
        application/vnd.ms-fontobject
        image/svg+xml;

    # 主路径
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 静态资源缓存（图片、CSS、JS等）
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|woff|woff2|ttf|svg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # 允许大文件上传（如果需要）
    client_max_body_size 100M;
}

# 如果只有IP没有域名，使用这个配置
# server {
#     listen 443 ssl http2;
#     server_name your-server-ip;  # 替换为服务器IP
#
#     ssl_certificate /etc/letsencrypt/live/your-server-ip/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-server-ip/privkey.pem;
#
#     root /var/www/particle-heart;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }

